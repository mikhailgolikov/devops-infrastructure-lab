db_user={{db_user}}
db_pass={{db_pass}}
db_name={{db_name}}
db_host={{db_host}}
db_port={{db_port}}


{% for node in redis_nodes%}
redis_node_{{loop.index}}={{node}}
{% endfor %}

redis_pass={{redis_pass}}
